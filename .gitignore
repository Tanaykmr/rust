# Binaries and intermediate files generated by Cargo
/target/

# If using Rustup (Rust version management), ignore toolchains and install logs
rustup-toolchain
**/*.rs.bk

# Cargo-specific files
Cargo.lock            # Only if you are creating a library, not a binary
/.cargo/

# Other common temporary files
**/*.rs.bk            # Backup files generated by some editors